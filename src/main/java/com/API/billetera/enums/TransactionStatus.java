package com.API.billetera.enums;

public enum TransactionStatus {
     /**
      * creamos los enum donde manejaremos los pendientes, completada y expirada
      */
    PENDING, // pendiente 
    COMPLETED, // completada
    EXPIRED // expirada 
}
